# MongoDB Configuration
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://chandanak1009_db_user:LeafyBooks2026@cluster0.k00qwf5.mongodb.net/bookstore?retryWrites=true&w=majority&authSource=admin&appName=Cluster0}
spring.data.mongodb.auto-index-creation=false

server.port=8080

# JWT Configuration
bookstore.app.jwtSecret=bookstoreSecretKeyForJWTSecurityPurposesMustBeLongEnoughToBeSecure
bookstore.app.jwtExpirationMs=86400000

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:sanapalachanikya@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


# Logging
logging.level.org.springframework.security=INFO
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.mongodb.driver=DEBUG

# Groq AI Configuration
groq.api.key=${GROQ_API_KEY:}

# File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Google Books API Key (Optional but recommended for bulk import)
# Get one at: https://console.cloud.google.com/apis/credentials
google.books.api.key=${GOOGLE_BOOKS_API_KEY:}

# Frontend Connection
app.frontend.url=${FRONTEND_URL:https://virtual-book-store-eight.vercel.app}
